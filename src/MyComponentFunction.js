import MyComponent from './MyComponent';
import React from 'react';
const MyComponent = ({name})=>
        {return (
            <div>함수형 컴포넌트 이름을 받았습니다. {name}</div>
        );
        }
export default MyComponent 